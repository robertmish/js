// Javascript data types

// Strings
const name = "Robert";

// Number
const age = 34;

// Bolean
let status = true;

// Null
const totalNumber = null;

// Symbol

const heart = Symbol("Robert");

// Printing the values of variables

const x = Symbol("hey");

console.log(x.description);

// Objects

const userInfo = {
  name: "robert",
  age: 34,
  status: true,
  hobbie: null,
};

const sentence = `Robert is ${age} and his marriage status is ${status}`;

console.log(sentence);
