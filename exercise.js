// Data types

// Numbers or integers

let amount = 3;
const amounts2 = 20;

let = 40;

// String

let word = "This is a word";

const words = "Hey, there, here, amount, bla bla";

// Boolean  (True or false)

let status = false;

// JavaScript Objects

const user = {
  name: "Robert",
  age: 23,
  married: true,
};

// Object with a function inside

const car = {
  brand: "Volvo",
  horsePower: 223,
  year: 2019,
  greeting: function () {
    console.log("Hey there");
  },
};

/// returns the bigger

const biggerNumber = function (num1, num2) {
  if (num1 > num2) {
    return num1;
  } else {
    return num2;
  }
};

// execute a function

// console.log(biggerNumber(100, 50));

// program to generate random strings

// const result = Math.random().toString(36).substring(2, 20);

// console.log(result);
